{
  "name": "@mio/synthetic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Deterministic synthetic-data generator - the only data allowed outside production (E8)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "node --import @swc-node/register/esm-register src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "seed": "node --import @swc-node/register/esm-register src/seed-cli.ts"
  },
  "devDependencies": {
    "@mio/authz": "workspace:*",
    "@mio/contracts": "workspace:*",
    "@mio/survey-schema": "workspace:*",
    "@swc-node/register": "^1.12.1",
    "@types/node": "^26.2.0",
    "@types/pg": "^8.23.1"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.1.0",
    "pg": "^8.23.0"
  }
}
