{
  "name": "@mio/authz",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Authorization primitives; Cedar toolchain lands in WP-04",
  "exports": {
    ".": "./src/index.ts",
    "./engine": "./src/engine.ts",
    "./matrix": "./src/matrix.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "generate": "node --import @swc-node/register/esm-register src/generate.ts",
    "generate:check": "node --import @swc-node/register/esm-register src/generate.ts --check"
  },
  "dependencies": {
    "@cedar-policy/cedar-wasm": "^4.12.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@swc-node/register": "^1.12.1",
    "@types/node": "^26.2.0"
  }
}
