/**
 * The Mio design system (docs/design/design-system.md). Tokens and the
 * component kit land in WP-03; the package exists now so app imports and
 * boundary rules are stable from the first commit.
 */

export const DESIGN_SYSTEM = 'warm-editorial' as const;
